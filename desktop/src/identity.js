/**
 * Names the app goes by, shared by the main process and the build config.
 * The version and the public site's address live in desktop/package.json.
 */

/**
 * Reverse-DNS id. On Windows it ties the running window to its Start menu
 * shortcut, on macOS it is the bundle id, and on Linux it names the package.
 * Changing it makes the app a different app to all three.
 */
export const APP_ID = 'io.github.jarvisar.orrery';

export const PRODUCT_NAME = 'Orrery';

export const SOURCE_URL = 'https://github.com/jarvisar/orrery';

/** The page's own background, so the window never flashes white while it loads. */
export const BACKGROUND = '#05070f';
